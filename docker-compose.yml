services:
  client:
    build:
      context: ./src/client
      dockerfile: Dockerfile
    ports:
      - "5501:8080"
    depends_on:
      - server
    networks:
      - expense-tracker_network

  server:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    networks:
      - expense-tracker-network


networks:
  expense-tracker_network:
    driver: bridge